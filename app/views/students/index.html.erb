<h1>All Students</h1>

<div>
  <% @students.each do |student| %>
    <div><%= "#{student.first_name} #{student.last_name}     (#{student.active== true ? "Active" : "Inactive"})" %> &emsp; <%= link_to 'View profile', student_path(student.id) %></div><br>
  <% end %>
</div>
